# プロジェクト一覧セクション実装

## 概要
ポートフォリオのプロジェクトをカード形式で表示するセクションを作成します。

## プロンプト

```
/ralph-loop "プロジェクト一覧セクションを実装して。

## タスク
ポートフォリオのプロジェクトをカード形式で表示するセクションを作成。

## 要件
- プロジェクトカードコンポーネント
  - サムネイル画像
  - タイトル
  - 説明文（2行まで）
  - 技術スタックタグ
  - GitHubリンク / デモリンク
- グリッドレイアウト（レスポンシブ）
- ホバー時のアニメーション効果
- フィルタリング機能（技術スタック別）

## プロジェクトデータ
既存のプロジェクトデータがあれば使用、なければサンプルデータを作成。

## 成功基準
- [ ] カードが正しくグリッド表示される
- [ ] ホバーアニメーションが動作する
- [ ] フィルターが機能する
- [ ] 画像の遅延読み込みが実装されている
- [ ] リンクが正しく動作する

## 10イテレーション後も未完了の場合
- 動作する部分のみでコミット
- 未実装機能をTODOコメントで記載
- 次のステップを提案

完了したら <promise>PROJECTS_DONE</promise> と出力。" --max-iterations 20 --completion-promise "PROJECTS_DONE"
```

## 期待される成果物
- `components/Projects.tsx` または `components/projects/index.tsx`
- `components/ProjectCard.tsx`
- `data/projects.ts` - プロジェクトデータ
